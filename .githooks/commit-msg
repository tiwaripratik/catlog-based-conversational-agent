#!/bin/sh
# commit-msg hook that forces every commit message to 'final commit'
# Usage: Git runs this script automatically with the path to the commit message file

if [ -z "$1" ]; then
  echo "No commit message file supplied"
  exit 1
fi

echo "final commit" > "$1"
exit 0
